version: '3.8'

services:
  # Frontend Service
  # frontend:
    # build:
    #   context: ./UserAccessManager
    #   dockerfile: Dockerfile
    # container_name: fertiwell-frontend
    # ports:
    #   - "5173:5173"
    # environment:
    #   - NODE_ENV=development
    #   - VITE_API_URL=http://localhost:5000
    # volumes:
    #   - ./UserAccessManager:/app
    #   - /app/node_modules
    # networks:
    #   - fertiwell-network
    # depends_on:
    #   - backend
    # restart: unless-stopped

  # Backend Service
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fertiwell-backend
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=development
      - PORT=5000
    volumes:
      - .:/app
      - /app/node_modules
      - ./serviceAccountKey.json:/app/serviceAccountKey.json:ro
    networks:
      - fertiwell-network
    restart: unless-stopped

networks:
  fertiwell-network:
    driver: bridge

volumes:
  backend_node_modules:
  # frontend_node_modules:
